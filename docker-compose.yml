version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: coin-collection-db
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme123}
      POSTGRES_DB: ${DB_DATABASE:-coin_collection}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - coin-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: coin-collection-backend
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-changeme123}@postgres:5432/${DB_DATABASE:-coin_collection}?schema=public
      JWT_SECRET: ${JWT_SECRET:-changeme123-change-this-in-production}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost}
    ports:
      - "3000:3000"
    volumes:
      - backend_uploads:/app/uploads
      - backend_thumbnails:/app/uploads/thumbnails
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - coin-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000}
    container_name: coin-collection-frontend
    ports:
      - "80:80"
      - "5173:5173"
    environment:
      VITE_API_URL: ${VITE_API_URL:-http://localhost:3000}
    networks:
      - coin-network
    depends_on:
      - backend

volumes:
  postgres_data:
  backend_uploads:
  backend_thumbnails:

networks:
  coin-network:
    driver: bridge